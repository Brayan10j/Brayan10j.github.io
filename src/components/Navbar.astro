---
import { ModeToggle } from "./ModeToggle";
---

<header class="sticky top-0 z-50 w-full border-b border-border/50 bg-background/80 backdrop-blur-md transition-all">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between gap-4">
    {/* Brand / Logo */}
    <a href="#" class="flex items-center gap-2.5 group">
      <span class="text-lg">⚡</span>
      <div class="flex items-baseline gap-2">
        <span class="font-heading font-bold text-base sm:text-lg tracking-tight text-foreground group-hover:text-primary transition-colors">
          Brayan Espitia
        </span>
        <span class="hidden sm:inline-block text-xs text-muted-foreground font-mono">
          / AI & FullStack
        </span>
      </div>
    </a>

    {/* Navigation Links */}
    <nav class="hidden md:flex items-center gap-6 text-sm font-medium text-muted-foreground">
      <a href="#about" class="hover:text-foreground transition-colors">
        Sobre Mí
      </a>
      <a href="#skills" class="hover:text-foreground transition-colors">
        Skills
      </a>
      <a href="#projects" class="hover:text-foreground transition-colors">
        Proyectos
      </a>
      <a href="#experience" class="hover:text-foreground transition-colors">
        Experiencia
      </a>
      <a href="#contact" class="hover:text-foreground transition-colors">
        Contacto
      </a>
    </nav>

    {/* Actions */}
    <div class="flex items-center gap-3">
      <a
        href="#contact"
        id="nav-contact-btn"
        class="inline-flex items-center gap-1.5 px-3.5 py-1.5 rounded-lg bg-foreground text-background text-xs font-semibold hover:opacity-90 active:scale-95 transition"
      >
        <span>Contacto</span>
        <span>✉️</span>
      </a>
      <ModeToggle client:load />
    </div>
  </div>
</header>

